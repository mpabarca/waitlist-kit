import type { ThemeType } from "../types/theme";

export const defaultTheme: ThemeType = {
  id: 'default',
  name: 'Default',
  font: 'Inter, sans-serif',
  background: '#ffffff',
  textColor: '#1a1a1a',
  primaryColor: '#2563eb',
  accentColor: '#4f46e5',
  spacing: 'relaxed',
}